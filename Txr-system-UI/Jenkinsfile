

pipeline {
    agent any
    stages {
        stage('build') { 
                steps {
                    dir("Txr-system-UI") {
                        sh 'npm install' 
                    }
                }
            }
        }
}