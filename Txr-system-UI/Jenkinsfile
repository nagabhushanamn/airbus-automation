

pipeline {
    agent any
    stages {
        stage('install dependencies') { 
                steps {
                    dir('Txr-system-UI') {
                        sh 'npm install' 
                    }
                }
            }
    }
}